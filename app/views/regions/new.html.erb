<%= link_to '< Back', image_regions_path(params[:image_id]) %>

<h1>New region</h1>
<br>
<%= form_for(@region, :url => image_regions_path) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
<% end %>
