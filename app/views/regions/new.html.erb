<%= link_to '< Back', image_regions_path(params[:image_id]) %>

<h1>New region</h1>
<br>
<%= form_for(@region, :url => image_regions_path, :html => { :id => "region_form" }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
<% end %>
