<%= link_to '< Back', region_path(@region) %>

<h1>Editing region</h1>

<%= form_for(@region, :html => { :id => "region_form" }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
<% end%>
